[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = {version = "0.5.0-rc.2", features = ["json"]}
serde = "1.0.136"
chrono = "0.4"
rand = "0.8"
tokio = { version = "1.20.1", features = ["full"] }